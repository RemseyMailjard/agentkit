# Instructor Guide

Start simple, demonstrate before guided practice, then move to independent work.

Use fallback demos when local MCP, package restore, authentication or network access is blocked. Never claim a lab environment is verified unless it was actually tested.
