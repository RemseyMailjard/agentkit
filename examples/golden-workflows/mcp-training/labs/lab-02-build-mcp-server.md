# Lab 02 — Build an MCP Server

Goal: build a small capability-first MCP server.

Acceptance: participant implements a task-oriented capability, validates input, returns structured output and handles at least one backend failure.
