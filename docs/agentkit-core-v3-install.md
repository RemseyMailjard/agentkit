# AgentKit Core v3 — Integration

Add `plugins/agentkit-core` to the AgentKit marketplace manifest as a local plugin.

Recommended category: Productivity.

Before release:
1. run structural validation;
2. run consolidated routing evals;
3. run official Plugin Eval;
4. review semantic-overlap report;
5. tune token/context budget;
6. keep OneNote and other specialized plugins as separate domain plugins where their external integration boundary matters.
